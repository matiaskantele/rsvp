import { useTranslation } from "react-i18next";

import Form from "../components/Form";

const RSVP = () => {
  const { t } = useTranslation();

  return (
    <>
      <Form />
    </>
  );
};

export default RSVP;
