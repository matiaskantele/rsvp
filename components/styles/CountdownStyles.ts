import styled from 'styled-components';

export const CountdownContainer = styled.div`
  position: absolute;
  bottom: 5rem;
  font-size: 0.8rem;
  @media (min-width: 700px) {
    font-size: 1.2rem;
  }
`;
